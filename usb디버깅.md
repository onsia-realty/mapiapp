# USB 디버깅 & 실기기 캡처 가이드 (노트북 환경 이어받기용)

> 개발사 마피 앱을 실기기에서 화면 캡처해 온시아 웹 데모 재현/회의 레퍼런스로 쓰기 위한 ADB 환경 설정 + 작업 이어가기 핸드오프 문서.
> 다른 PC(노트북)에서 저장소를 clone 한 뒤 이 문서만 따라 하면 캡처 작업을 그대로 이어갈 수 있음.

---

## 1. 사전 준비 (노트북에서 최초 1회)

### ADB 설치
- Android SDK Platform-Tools 설치 → `adb.exe` 경로 확보.
- 기존 PC 경로: `C:\Users\Dae\AppData\Local\Android\Sdk\platform-tools\adb.exe`
- 노트북에선 설치 경로가 다를 수 있으니 `adb` 경로를 확인하고 `_cap.ps1` / `_tap.ps1` 안의 `$adb` 값을 수정.
- 설치 확인: `adb version`

### 기기 (갤럭시 Z폴드 SM-F956N, Android 16)
1. 설정 → 휴대전화 정보 → 소프트웨어 정보 → **빌드번호 7번 탭** → 개발자 옵션 활성화.
2. 설정 → 개발자 옵션 → **USB 디버깅 ON**.
3. USB 케이블 연결 → 폰에 뜨는 "USB 디버깅 허용" 팝업 **허용**(이 컴퓨터에서 항상 허용 체크).
4. 연결 확인: `adb devices` → `device` 상태로 보여야 함(`unauthorized`면 폰 팝업 재승인).

### 화면 꺼짐 방지
```
adb shell settings put global stay_on_while_plugged_in 3
```

### (선택) 삼성 Flow 미러링
- PC에서 앱 화면을 실시간으로 보며 좌표를 잡을 때 유용. 미러링 중 "자동 회전 불가" 토스트는 무시.

---

## 2. 캡처 워크플로우 (핵심 주의사항)

### ⚠️ 디스플레이가 2개
Z폴드는 디스플레이가 2개라 기본 screencap이 검게 나올 수 있음. **커버 화면 display id `4630947194243491972`**(968×2376, density 420)를 지정해야 함.
```
adb shell screencap -p -d 4630947194243491972 /sdcard/cap.png
```

### ⚠️ Git-bash 금지 → PowerShell 사용
Git-bash는 `/sdcard/` 경로를 Windows 경로로 왜곡시켜 pull이 실패함. **screencap/pull은 반드시 PowerShell로 실행.**

### ⚠️ 이미지 읽기 2000px 한계
968×2376 원본을 그대로 여러 장 읽으면 표시 한계(2000px)에 걸림. `_thumb.ps1`로 760px 썸네일을 만들어 그걸 읽고, 원본은 풀해상도로 보존.

### 헬퍼 스크립트 (`docs/개발사앱_원본캡처/`)
- `_cap.ps1 -name <이름>` : 지정 display 캡처 + pull → `<이름>.png`
- `_tap.ps1 -x <x> -y <y>` : 좌표 탭 (1.5초 대기)
- `_thumb.ps1 -name <이름>` : `_thumbs/<이름>.png` 760px 썸네일 생성
- **노트북에선 `_cap.ps1`/`_tap.ps1`의 `$adb` 경로를 노트북 adb 경로로 수정 필요.**

### 앱 재실행 (앱 이탈 시)
```
adb shell monkey -p com.knowledgeware.modelexecutor80.mapi_1450484161 -c android.intent.category.LAUNCHER 1
```

---

## 3. 좌표 노트 (968×2376 기준)

- **하단 nav 5탭 (y≈2220)**: 홈 98 / 관심목록 292 / 매물등록 484 / 구인구직 680 / 더보기 874
- **지도 카테고리 화면(아파트·오피스텔·사무실·상가)**: 하단 "홈" 탭·시스템 뒤로가기가 **동작 안 함** → **헤더 좌상단 ← (70,172)** 탭해야 이전으로.
- **마피 매물보기 핑크버튼**: (484,2010) 근처 (2220은 매물등록 nav탭이라 오탭 주의).
- **등록 폼은 전체화면 모달**이라 하단 nav 없음 → 맨 위로 스크롤해 헤더 ←로 빠져나감.

---

## 4. 테스트 계정 (`.env.local`의 MAPI_TEST_*)
- 중개사: `jooong@google.com` / PW `1`
- 분양사: `bys@google.com` / PW `1`
- 일반: `test@mapi.com` / PW `1`
- 로그인은 사용자가 직접 수행. (SmartMaker 씬 클라이언트라 자동화 로그인 어려움)

---

## 5. 작업 이어가기 (핸드오프)

### 지금까지 완료
- **APK 분석**: `mapi_apk.apk`는 SmartMaker(엠비즈메이커) 노코드 씬 클라이언트. 내부에 화면/데이터 없음 → **역분석 불가**, 실기기 관찰이 유일한 방법.
- **캡처 스윕 완료**: 39개 화면 원본 아카이브 → `docs/개발사앱_원본캡처/` (인덱스 `README.md`, 회의 지적 정리 `_회의_지적사항.md`).
  - 홈 / 분양권목록·단지상세 / 마피목록·상세 / 아파트지도·바텀시트·상세 / 오피스텔·사무실·상가(빈지도) / 분양권·이자만 의뢰폼 / 관심목록 / 구인구직 / 더보기·내정보 / 매물등록폼·조회.

### 발견한 버그 (회의 지적용)
1. 마피 개별상세 주변시세 "현재 매물 **36억 6,000만원**"(억 단위 계산오류, 3억6천이어야).
2. 더보기 "중개사 가입/광고안내" 버튼이 앱 화면 아닌 **외부 `m.naver.com`으로 잘못 연결**.
3. 결제/요금제 미구현(더보기·마이페이지 모두). 마이페이지는 SNS로그인·번호재설정·로그아웃뿐.

### 다음 할 일 후보
- 오피스텔/사무실/상가 등록 폼, 더보기 하위(고객센터·공지·약관) 추가 캡처(선택).
- 이 아카이브와 `onsia-web` 데모를 비교해 부족한 부분 정리 → 회의 자료화.

### 프로젝트 컨텍스트 메모 (저장소에 복사됨)
Claude Code 메모리는 PC 로컬에 저장돼 clone 시 따라오지 않으므로, 핵심 메모를 `docs/작업메모/`에 복사해 둠. 노트북에서 참고.
- `docs/작업메모/project_mapi_apk_analysis.md`
- `docs/작업메모/project_mapi_live_app_capture.md`
- `docs/작업메모/project_3party_structure.md`
- `docs/작업메모/project_devco_pending_items.md`
